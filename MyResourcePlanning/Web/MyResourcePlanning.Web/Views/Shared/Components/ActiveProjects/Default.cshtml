@model IEnumerable<MyResourcePlanning.Web.ViewModels.Project.ProjectAllViewModel>

<option selected disabled value="">Project (Hours);Period</option>
@foreach (var option in Model)
{
    <option>@option.Name (@option.RequestedHours) ;@option.StartDate -> @option.EndDate</option>
}

